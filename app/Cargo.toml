[package]
name = "app"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "chess"
path = "src/main.rs"

[dependencies]
# Local crates
engine = { path = "../engine" }

# External packages
bevy = { version = "0.17" }
rand = "0.9.2"
